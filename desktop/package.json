{
  "name": "godot-mcp-desktop",
  "version": "2.0.2",
  "private": true,
  "description": "Desktop app for managing the Godot MCP server: console, start/stop, agent auto-configuration",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
